<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-12'>
  　   <table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td></td>
        <td><%= user.name %></td>
        <td>フォロー数:<%= user.relationships.count %></td>
        <td>フォロワー数:<%= user.relationships.count %></td>
        <td><%= render "user/relationships/follow", user: user %></td>
        <td><%= link_to 'Show', user_user_path(user.id), class: "user_#{user.id}" %></td>
      </tr>
    <% end %>
  </tbody>
      </table>
    </div>
  </div>
</div>