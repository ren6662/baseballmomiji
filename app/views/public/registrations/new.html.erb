<div class= "container">
  <div class ="row" style="margin-left: 300px; margin-right: 300px;">
    <div class="border mt-4" style="width: 500px;	height: 750px;">
      <div class="col-12">
        <h2 class= "text-center mt-5 mb-3 ml-3 font-weight-bold">新規登録</h2>
        <p class="text-center">下記のフォームに必要事項を入力し<br>
        「登録する」を押してください。<br>
        ※プロフィール画像は必須ではありません
        </p>
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "public/shared/error_messages", resource: resource %>

        <div class="field text-center">
          <%= f.text_field :name, autofocus: true, autocomplete: "name", class:'col-sm-9 ml-3', placeholder:"名前" %>
        </div>

        <div class="field text-center mt-4">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'col-sm-9 ml-3', placeholder:"メールアドレス" %>
        </div>

        <div class="field text-center mt-4">
          <%= f.label :プロフィール画像 %><br />
          <%= f.file_field :image, autofocus: true, autocomplete: "image" %>
        </div>

        <div class="field text-center mt-4">
          <%= f.password_field :password, autocomplete: "new-password", class:'col-sm-7 ml-3', placeholder:"パスワード(半角6文字以上)" %>
        </div>

        <div class="field text-center mt-4">
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class:'col-sm-7 ml-3', placeholder:"パスワード再入力" %>
        </div>

        <div class="actions text-center">
          <%= f.submit "登録する", class: 'mt-3 btn btn-success w-50' %>
        </div>
      <% end %>
      
      <h5 class= "text-center mt-5 mb-3 font-weight-bold">会員登録がお済みの方</h5>
       <p class= "text-center mt-3 mb-3 font-weight-bold"><%= link_to "こちら", new_user_session_path, class:'text-info ' %>からログインを行なってください。</p>


    </div>
   </div>
  </div>
</div>